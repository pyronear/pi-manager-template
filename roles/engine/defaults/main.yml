---
# Pinned engine image version, shared by the pyro-engine and pyro-camera-api
# containers. Environments that must track the newest build (preprod) override
# pyro_engine_docker_tag in the sister repo's group_vars.
pyro_engine_docker_tag: "1.0.13"
