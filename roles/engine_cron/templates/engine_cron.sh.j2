#!/bin/bash

set -e

echo "[INFO] Pulling latest code"
git pull origin {{ engine_branch }}

echo "[INFO] Building Docker image"
docker build -t {{ engine_cron_docker_image_name }} .

echo "[INFO] Restarting docker-compose"
docker compose down
docker compose up -d
